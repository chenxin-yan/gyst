{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".crust/**"]
    },
    "//#lint": {},
    "//#format": {},
    "topo": {
      "dependsOn": ["^topo"]
    },
    "check:types": {
      "dependsOn": ["topo"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
