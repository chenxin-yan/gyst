---
"@gyst/cli": patch
---

Press Enter to step into the current item and j / k to move between a group's hunks; Esc returns to the list. The focused hunk is shared with the agent as `cursor.hunkId` in `gyst session status`.
