---
"@gyst/cli": minor
---

Upgrade Crust to 0.4.0 (`@crustjs/core`, `@crustjs/extensions`, `@crustjs/skills`, `@crustjs/crust`) and `@crustjs/effect` to 0.1.2. Breaking: `gyst skills update` is replaced by `gyst skills repair`. `gyst skills install` (same as the root `gyst skills`) and `gyst skills uninstall` are new.
